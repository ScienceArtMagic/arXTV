{
  "name": "server",
  "type": "module",
	"scripts": {
		"dev": "bun run --hot index.ts",
		"start": "bun run index.ts"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.654.0",
    "hono": "^4.6.3"
  }
}